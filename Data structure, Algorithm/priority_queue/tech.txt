heapq 의 디폴트 정렬은 min heap (작은 수가 루트)
max heap 은 요소를 - 취하면 된다
튜플과 같이 요소 안에 수가 여러개 있으면 제일 앞에 있는 요소를 기준으로 정렬된다

우선순위 큐는 정렬 상태를 유지하면서 삽입, 삭제 연산을 자주할 때 유리하다 (ex_01417)
또한 정렬 상태를 유지하면서 삽입, 삭제 연산에 특화되어 있기 때문에
입력 데이터를 필요한 만큼만 갱신하면서 받으면 메모리를 줄일 수 있다 (ex_02075)